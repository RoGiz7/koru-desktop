# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build (artefactos pesados, no van al repo)
src-tauri/target/

# Base de datos local y datos del usuario (no van al repo)
*.sqlite
*.sqlite3
*.db

# Datos de juego personales (gamelogs, chatlogs, research con datos reales): NUNCA al repo.
# `documentacion/` es hermana de koru-desktop (fuera de este repo) y no se sube; este candado
# es por si algún día se mueve dentro o se crea un repo en la carpeta padre.
documentacion/
**/Gamelogs/
**/chat logs/
**/Chatlogs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
